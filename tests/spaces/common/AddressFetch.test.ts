
//Commenting these out for now since I don't want to be hitting google's servers each time I run some tests
/*
test('convertAddressUrlToCoordinates returns correct value', async () => {
    const coords = await convertAddressUrlToCoordinates('https://maps.app.goo.gl/SvuJ3Jb3sW6nbFyi8');
    expect(Number(coords.longitude)).toBeCloseTo(-0.7377184, 5);
    expect(Number(coords.latitude)).toBeCloseTo(52.053968, 5);
});

test('convertAddressUrlToCoordinates returns correct value 2', async () => {
    const coords = await convertAddressUrlToCoordinates('https://maps.app.goo.gl/MVDySsNNkiLchpr17');
    expect(Number(coords.longitude)).toBeCloseTo(-0.7377184, 5);
    expect(Number(coords.latitude)).toBeCloseTo(52.053968, 5);
});
*/